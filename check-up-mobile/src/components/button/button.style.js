import { COLORS, FONT_SIZE } from '../../constants/theme.js';

export const styles = {
	btn: {
		width: "100%",
		backgroundColor: COLORS.purple2,
		borderRadius: 6,
		padding: 12,
	},

	text: {
		color: "#FFF",
		fontSize: FONT_SIZE.md,
		textAlign: "center",

	}

}