import { COLORS, FONT_SIZE } from '../../constants/theme.js';

export const styles = {
	btn: {
		width: "100%",
		backgroundColor: COLORS.green3,
		borderRadius: 6,
		padding: 12,
	},

	text: {
		color: "#FFF",
		fontSize: FONT_SIZE.md,
		textAlign: "center",
	}

}